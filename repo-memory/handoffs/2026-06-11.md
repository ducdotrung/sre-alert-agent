# Handoff - 2026-06-11

## Session Summary

- Removed the legacy `agents/` folder after migrating all remaining runtime, test, script, and docs references to the shared `alert_agent` pipeline modules.
- Updated manual review dispatch to invoke `alert_agent.pipeline.recommendation` and `alert_agent.pipeline.sender` directly.
- Updated operational scripts and public docs to use `python3 -m alert_agent.pipeline.*` commands instead of the deleted wrapper files.
- Replaced legacy-wrapper unit imports with direct imports from `alert_agent.core` and `alert_agent.pipeline`.

## Verification

- `python3 -m unittest tests.test_manual_review tests.test_recommendation_sender tests.test_review_notifications`
- `rg -n "agents/|from agents|import agents|agents\\.|from shared|import shared" -S .`

## Notes

- The remaining `agents` text in the repo is only in historical repo-memory notes and generic wording like "all agents" in documentation, not in executable paths or imports.
