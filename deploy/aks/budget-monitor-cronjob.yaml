apiVersion: batch/v1
kind: CronJob
metadata:
  name: sre-alert-budget-monitor
spec:
  schedule: "15 * * * *"
  timeZone: "Etc/UTC"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          securityContext:
            fsGroup: 1000
          containers:
            - name: monitor
              image: ghcr.io/example/sre-alert-agent:1.0
              imagePullPolicy: IfNotPresent
              command:
                - /bin/bash
                - -lc
                - python3 scripts/check_ai_budget.py --config "${CONFIG_FILE}"
              envFrom:
                - configMapRef:
                    name: sre-alert-agent-config
                - secretRef:
                    name: sre-alert-agent-secrets
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
              volumeMounts:
                - name: output
                  mountPath: /app/output
          volumes:
            - name: output
              persistentVolumeClaim:
                claimName: sre-alert-agent-output
